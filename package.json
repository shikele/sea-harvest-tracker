{
  "name": "sea-harvest-tracker",
  "version": "1.0.0",
  "description": "Washington State shellfish harvesting tracker with biotoxin status, tide predictions, harvest calendars, and community comments",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "build": "npm install --include=dev --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "cd backend && node src/index.js"
  },
  "engines": {
    "node": "20.x"
  }
}
